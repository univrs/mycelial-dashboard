# Mycelial Dashboard Configuration
# Copy this file to .env.local and adjust values for your environment

# WebSocket URL for real-time updates (P2P network)
VITE_WS_URL=ws://localhost:8080/ws

# REST API URL for peer queries (P2P network)
VITE_API_URL=http://localhost:8080

# ===========================================
# Orchestrator Configuration
# ===========================================

# Orchestrator WebSocket URL (defaults to VITE_WS_URL if not set)
# VITE_ORCHESTRATOR_WS_URL=ws://localhost:8080/ws

# Orchestrator REST API URL (defaults to VITE_API_URL if not set)
# VITE_ORCHESTRATOR_API_URL=http://localhost:8080/api

# Enable mock data mode for development (true/false or 1/0)
# When enabled, the dashboard will use simulated data instead of real API
# Mock data will also auto-enable after 3 consecutive API failures as fallback
VITE_USE_MOCK_DATA=true
